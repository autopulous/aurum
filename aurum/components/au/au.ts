import {Component} from '@angular/core';

const SELECTOR: string = 'au';

@Component({
  selector: SELECTOR,
  template: require('./au.html'),
  styles: [require('./au.scss')]
})
export class AuComponent {
}