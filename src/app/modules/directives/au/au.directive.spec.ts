import {AuDirective} from './au.directive';

describe('AuDirective', () => {
  it('should create an instance', () => {
    const directive = new AuDirective();
    expect(directive).toBeTruthy();
  });
});
